# Warehouse Management System (WMS)

ระบบจัดการคลังสินค้าแบบ Web Application พัฒนาด้วย Node.js, Express และ SQLite เน้นความเรียบง่าย ใช้งานได้จริง และมี UI ที่ทันสมัย (Minimal & Clean)

## 📌 คุณสมบัติเด่น (Features)

- **ระบบจัดการสินค้าคงคลัง (Inventory Management)**
  - ค้นหาสินค้าได้แบบ Real-time ตามชื่อหรือหมวดหมู่
  - สลับมุมมองการแสดงผลได้ทั้งแบบ Grid (การ์ด) และ Table (ตาราง)
  - แบ่งหน้าจอแสดงผลสินค้า (Pagination) หน้าละ 100 รายการ
  - ระบบแจ้งเตือนสินค้าใกล้หมดอายุ (ภายใน 2 วัน) จะมีการไฮไลท์สีให้เห็นชัดเจน
- **ระบบรับเข้าและเบิกจ่าย (Inbound / Outbound)**
  - เพิ่มสต็อกเข้าคลัง พร้อมระบุ วันรับเข้า และระบบจะคำนวณวันหมดอายุให้อัตโนมัติ (ตามอายุขัยของสินค้า)
  - ระบบตัดจ่ายแบบ **FIFO (First In, First Out)** อัตโนมัติ โดยจะดึงล็อตที่เก่าที่สุดก่อน
- **ระบบจัดการข้อมูลสำหรับผู้ดูแลระบบ (Admin Dashboard)**
  - **จัดการสินค้า:** เพิ่ม ลบ แก้ไข ข้อมูลสินค้า (Full CRUD) พร้อมหมวดหมู่ให้เลือก และระบบค้นหา
  - **จัดการผู้ใช้งาน:** เปลี่ยน Role (Admin/User), รีเซ็ตรหัสผ่าน, ลบผู้ใช้ (ห้ามลบตัวเอง)
- **ระบบประวัติการทำรายการ (Transaction History)**
  - บันทึกทุกรายการรับเข้าและเบิกออก ระบุผู้ทำรายการ วันเวลา และจำนวน
  - แบ่งหน้าประวัติ (Pagination) หน้าละ 100 รายการ
- **ระบบรักษาความปลอดภัย (Security)**
  - เข้าสู่ระบบด้วย username & password (เข้ารหัสด้วย bcryptjs)
  - มี Role สำหรับแบ่งระดับผู้ใช้งาน (User ทั่วไปเบิกของพอดูสต็อกได้, Admin สามารถจัดการสินค้าและผู้ใช้งานได้)

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

**Backend:**
- Node.js & Express.js
- SQLite3 (Database เก็บเป็นไฟล์ `.sqlite` ง่ายต่อการใช้งานไม่ต้องตั้งค่า server แยก)
- bcryptjs (สำหรับเข้ารหัสผ่าน)
- express-session (สำหรับจัดการ Session)

**Frontend:**
- EJS (Template Engine)
- Tailwind CSS (สำหรับจัดการ UI Layout)
- SweetAlert2 (สำหรับแสดง Popup / Toast Notification)

## 🚀 วิธีการติดตั้งและรันโปรเจกต์ (Installation & Setup)

1. **Clone Repository หรือดาวน์โหลดโค้ด**
   ```bash
   git clone https://github.com/nuntawatrod/webpro_wms.git
   cd webpro_wms
   ```

2. **ติดตั้ง Dependencies**
   (ต้องมี Node.js ติดตั้งอยู่ในเครื่องก่อน)
   ```bash
   npm install
   ```

3. **เตรียมฐานข้อมูลและรันเซิร์ฟเวอร์**
   ```bash
   npm run dev
   ```
   *หมายเหตุ: เมื่อรันครั้งแรก ระบบจะทำการสร้างฐานข้อมูล `database.sqlite` พร้อมตารางและข้อมูลตั้งต้น (รวมถึง User Admin ตั้งต้น) อัตโนมัติ*

4. **การเข้าใช้งาน**
   เปิดเบราว์เซอร์ไปที่: `http://localhost:3000`

## 👤 บัญชีผู้ใช้งานเริ่มต้น (Default Account)

เมื่อรันระบบครั้งแรก สามารถล็อกอินด้วยบัญชี Admin เริ่มต้นได้:
- **Username:** `admin`
- **Password:** `123456`
*(แนะนำให้เปลี่ยนรหัสผ่าน ทำได้โดยเข้าส่วนจัดการผู้ใช้ในเมนู Admin)*

---
*Developed with minimal UI design & playful interactions.*
