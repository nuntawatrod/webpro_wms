<!DOCTYPE html>
<html lang="th">
<%- include('layout/head') %>

    <body class="bg-slate-50 text-slate-800 antialiased min-h-screen flex items-center justify-center p-4">

        <div class="max-w-md w-full">
            <!-- Logo -->
            <div class="text-center mb-8">
                <h1
                    class="text-3xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-emerald-600 to-teal-500 mb-2">
                    คลังสินค้าอาหารสดและอาหาร</h1>
                <p class="text-slate-500">ลงชื่อเข้าสู่ระบบจัดการคลังสินค้า</p>
            </div>

            <div class="bg-white rounded-2xl shadow-xl border border-slate-100 p-8">

                <% if (error) { %>
                    <div
                        class="mb-6 bg-rose-50 border-l-4 border-rose-500 p-4 rounded text-sm text-rose-700 font-medium">
                        <%= error %>
                    </div>
                    <% } %>

                        <form action="/login" method="POST" class="space-y-6">
                            <div>
                                <label class="block text-sm font-medium text-slate-700 mb-1">ชื่อผู้ใช้
                                </label>
                                <input type="text" name="username" required autocomplete="off"
                                    class="w-full px-4 py-3 border border-slate-300 rounded-lg shadow-sm focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 outline-none text-sm transition-all"
                                    placeholder="กรอกชื่อผู้ใช้">
                            </div>

                            <div>
                                <label class="block text-sm font-medium text-slate-700 mb-1">รหัสผ่าน</label>
                                <input type="password" name="password" required
                                    class="w-full px-4 py-3 border border-slate-300 rounded-lg shadow-sm focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 outline-none text-sm transition-all"
                                    placeholder="กรอกรหัสผ่าน">
                            </div>

                            <div class="pt-2">
                                <button type="submit"
                                    class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-medium py-3 rounded-lg shadow-md hover:shadow-lg transition-all focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-emerald-500">
                                    เข้าสู่ระบบ
                                </button>
                            </div>
                        </form>

                        <!-- Note for demo purposes -->
                        <div class="mt-8 pt-6 border-t border-slate-100 text-center">
                            <p class="text-xs text-slate-400">บัญชีทดลอง: <span
                                    class="font-mono text-slate-500 font-medium">admin</span> / <span
                                    class="font-mono text-slate-500 font-medium">1234</span></p>
                        </div>

            </div>
        </div>

    </body>

</html>