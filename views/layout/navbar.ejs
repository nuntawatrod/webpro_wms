<!-- Navbar Component -->
<nav class="bg-white border-b border-slate-200 sticky top-0 z-40 shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex items-center">
                <span
                    class="text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-emerald-600 to-teal-500 hidden sm:block">ระบบจัดการคลังสินค้าอาหารสดและอาหาร</span>
                <span
                    class="text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-emerald-600 to-teal-500 sm:hidden">WMS</span>
            </div>
            <div class="flex items-center space-x-4 sm:space-x-8">
                <a href="/"
                    class="<%= locals.activePage === 'dashboard' ? 'text-emerald-600 border-b-2 border-emerald-600' : 'text-slate-500 hover:text-slate-700 hover:border-b-2 hover:border-slate-300' %> px-1 py-5 text-sm font-medium transition-colors">หน้าหลัก</a>

                <!-- Transactions Dropdown -->
                <div class="relative group">
                    <button
                        class="<%= (locals.activePage === 'withdraw' || locals.activePage === 'add' || locals.activePage === 'manage_products' || locals.activePage === 'manage_users') ? 'text-emerald-600 border-b-2 border-emerald-600' : 'text-slate-500 hover:text-slate-700 hover:border-b-2 hover:border-slate-300' %> px-1 py-5 text-sm font-medium transition-colors flex items-center gap-1">
                        ทำรายการ
                        <svg class="w-4 h-4 text-slate-400 group-hover:text-emerald-500 transition-colors" fill="none"
                            stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7">
                            </path>
                        </svg>
                    </button>
                    <!-- Dropdown Menu -->
                    <div
                        class="absolute left-0 mt-0 w-48 bg-white border border-slate-200 rounded-xl shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible group-hover:mt-1 transition-all duration-200 overflow-hidden z-50">
                        <div class="py-1">
                            <a href="/add-stock"
                                class="w-full text-left px-4 py-2 text-sm text-slate-700 hover:bg-emerald-50 hover:text-emerald-700 flex items-center gap-2">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M12 4v16m8-8H4"></path>
                                </svg>
                                เพิ่มสต็อก
                            </a>
                            <a href="/withdraw"
                                class="px-4 py-2 text-sm text-slate-700 hover:bg-emerald-50 hover:text-emerald-700 flex items-center gap-2">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M20 12H4M10 6L4 12L10 18"></path>
                                </svg>
                                เบิกสินค้า
                            </a>
                            <% if (user && user.role==='manager' ) { %>
                                <div class="border-t border-slate-100 my-1"></div>
                                <a href="/manage-products"
                                    class="px-4 py-2 text-sm text-slate-700 hover:bg-emerald-50 hover:text-emerald-700 flex items-center gap-2">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
                                    </svg>
                                    จัดการสินค้า
                                </a>
                                <a href="/manage-users"
                                    class="px-4 py-2 text-sm text-slate-700 hover:bg-emerald-50 hover:text-emerald-700 flex items-center gap-2">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z">
                                        </path>
                                    </svg>
                                    จัดการผู้ใช้งาน
                                </a>
                                <% } %>
                        </div>
                    </div>
                </div>
                <a href="/history"
                    class="<%= locals.activePage === 'history' ? 'text-emerald-600 border-b-2 border-emerald-600' : 'text-slate-500 hover:text-slate-700 hover:border-b-2 hover:border-slate-300' %> px-1 py-5 text-sm font-medium transition-colors">ประวัติ</a>

                <% if (user && user.role==='manager' ) { %>
                    <a href="/admin"
                        class="<%= locals.activePage === 'admin' ? 'text-emerald-600 border-b-2 border-emerald-600' : 'text-slate-500 hover:text-slate-700 hover:border-b-2 hover:border-slate-300' %> px-1 py-5 text-sm font-medium transition-colors">ผู้ดูแลระบบ</a>
                    <% } %>

                        <form action="/logout" method="POST" class="ml-4 pl-4 border-l border-slate-200 py-3 block">
                            <button type="submit"
                                class="text-sm font-medium text-rose-500 hover:text-rose-700 focus:outline-none flex items-center gap-1">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1">
                                    </path>
                                </svg>
                                ออกจากระบบ
                            </button>
                        </form>
            </div>
        </div>
    </div>
</nav>