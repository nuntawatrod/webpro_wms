<!DOCTYPE html>
<html lang="th">
<%- include('layout/head') %>

    <body class="bg-slate-50 text-slate-800 antialiased min-h-screen flex flex-col">

        <!-- Navbar -->
        <%- include('layout/navbar', { activePage: 'dashboard' , user: locals.user }) %>

            <!-- Main Content -->
            <main class="flex-grow max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full">

                <!-- Header Actions -->
                <div
                    class="flex flex-col md:flex-row justify-between items-start md:items-center mb-6 space-y-4 md:space-y-0">
                    <div>
                        <h1 class="text-2xl font-bold text-slate-900">คลังสินค้า</h1>
                        <p class="text-sm text-slate-500 mt-1">จัดการสินค้าอาหารสด</p>
                    </div>

                    <div class="flex items-center gap-3 flex-wrap">
                        <!-- View Toggle Buttons -->
                        <div class="bg-white p-1 rounded-lg border border-slate-200 flex shadow-sm">
                            <button id="btnGridView"
                                class="px-3 py-1.5 rounded-md text-sm font-medium bg-slate-100 text-slate-800 transition-colors focus:outline-none focus:ring-2 focus:ring-emerald-500">
                                <svg class="w-4 h-4 inline-block mr-1" fill="none" stroke="currentColor"
                                    viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z">
                                    </path>
                                </svg>
                                การ์ด
                            </button>
                            <button id="btnTableView"
                                class="px-3 py-1.5 rounded-md text-sm font-medium text-slate-500 hover:text-slate-800 transition-colors focus:outline-none focus:ring-2 focus:ring-emerald-500">
                                <svg class="w-4 h-4 inline-block mr-1" fill="none" stroke="currentColor"
                                    viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M4 6h16M4 10h16M4 14h16M4 18h16"></path>
                                </svg>
                                ตาราง
                            </button>
                        </div>

                        <!-- Search Box -->
                        <div class="relative">
                            <input id="productSearchBox" type="text" placeholder="ค้นหาสินค้า..."
                                class="pl-9 pr-4 py-2 bg-white border border-slate-200 rounded-lg text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 w-48 sm:w-56 transition-all">
                            <svg class="w-4 h-4 text-slate-400 absolute left-2.5 top-1/2 -translate-y-1/2 pointer-events-none"
                                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                            </svg>
                        </div>

                        <!-- Sort Dropdown -->
                        <div class="relative">
                            <select id="sortSelect"
                                class="appearance-none pl-3 pr-8 py-2 bg-white border border-slate-200 rounded-lg text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 text-slate-700 cursor-pointer transition-all">
                                <option value="name-asc">ชื่อ A → Z</option>
                                <option value="name-desc">ชื่อ Z → A</option>
                                <option value="expiry-asc">หมดอายุใกล้ก่อน</option>
                            </select>
                            <svg class="w-4 h-4 text-slate-400 absolute right-2 top-1/2 -translate-y-1/2 pointer-events-none"
                                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </div>

                        <button id="btnToggleExpired"
                            class="border border-slate-300 text-slate-600 px-4 py-2 rounded-lg text-sm font-medium hover:bg-slate-100 shadow-sm transition-all flex items-center focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-slate-400">
                            <svg class="w-4 h-4 mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            ดูสินค้าหมดอายุ
                        </button>

                    </div>
                </div>

                <!-- Notification Area -->
                <div id="toastContainer" class="fixed bottom-4 right-4 z-50 flex flex-col gap-2"></div>

                <!-- Expiry Warning Banner -->
                <div id="expiryBanner"
                    class="hidden mb-5 flex items-start gap-3 bg-rose-50 border border-rose-200 rounded-xl px-4 py-3 text-sm text-rose-800">
                    <svg class="w-5 h-5 text-rose-500 shrink-0 mt-0.5" fill="none" stroke="currentColor"
                        viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <div id="expiryBannerText" class="font-medium"></div>
                    <button id="btnDismissExpiry"
                        class="ml-auto text-rose-400 hover:text-rose-600 transition-colors focus:outline-none shrink-0">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                    </button>
                </div>

                <!-- Category Filter Tabs -->
                <div id="categoryTabs" class="flex flex-wrap gap-2 mb-5">
                    <!-- Dynamically injected from JS -->
                </div>

                <!-- Loading State -->
                <div id="loadingIndicator" class="flex justify-center items-center py-20 hidden">
                    <svg class="animate-spin -ml-1 mr-3 h-8 w-8 text-emerald-500" xmlns="http://www.w3.org/2000/svg"
                        fill="none" viewBox="0 0 24 24">
                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4">
                        </circle>
                        <path class="opacity-75" fill="currentColor"
                            d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z">
                        </path>
                    </svg>
                    <span class="text-slate-500 text-lg font-medium">กำลังโหลดข้อมูล (Loading...)</span>
                </div>

                <!-- Data Views Container -->
                <div id="dataContainer" class="w-full">

                    <!-- Grid View (Default) -->
                    <div id="gridView" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                        <!-- Cards injected here via JS -->
                    </div>

                    <!-- Table View -->
                    <div id="tableView"
                        class="hidden bg-white shadow-sm border border-slate-200 rounded-xl overflow-hidden">
                        <div class="overflow-x-auto">
                            <table class="w-full text-left border-collapse">
                                <thead>
                                    <tr
                                        class="bg-slate-50 border-b border-slate-200 text-xs uppercase tracking-wider text-slate-500 font-semibold">
                                        <th class="px-6 py-4 w-16">รูป</th>
                                        <th class="px-6 py-4">ชื่อสินค้า / หมวดหมู่</th>
                                        <th class="px-6 py-4 text-right">จำนวนรวม</th>
                                        <th class="px-6 py-4 text-center">สถานะ</th>
                                        <th class="px-6 py-4 w-16"></th>
                                    </tr>
                                </thead>
                                <tbody id="tableBody" class="divide-y divide-slate-100">
                                    <!-- Rows injected here via JS -->
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div id="emptyState"
                        class="hidden flex flex-col items-center justify-center py-20 bg-white border border-slate-200 rounded-xl shadow-sm">
                        <svg class="w-16 h-16 text-slate-300 mb-4" fill="none" stroke="currentColor"
                            viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                                d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4">
                            </path>
                        </svg>
                        <h3 class="text-lg font-medium text-slate-900">ไม่พบข้อมูลสินค้า</h3>
                        <p class="text-slate-500 mt-1 max-w-sm text-center">คลังสินค้าว่างเปล่า
                            หรือไม่มีข้อมูลตรงกับเงื่อนไข</p>
                    </div>

                </div>

                <!-- Pagination -->
                <div id="paginationContainer" class="mt-6"></div>

            </main>



            <!-- Custom Modal Overlay: Delete Confirm -->
            <div id="deleteConfirmModalOverlay"
                class="fixed inset-0 z-50 bg-slate-900/50 backdrop-blur-sm hidden flex items-center justify-center p-4">
                <div id="deleteConfirmModalContent"
                    class="bg-white rounded-3xl shadow-xl w-full max-w-md overflow-hidden animate-fade-in"
                    style="display: none;">

                    <div class="px-6 py-5 flex justify-between items-center bg-white">
                        <h2 class="text-lg font-bold text-slate-700">ยืนยันการลบ (Confirm Delete)</h2>
                        <button id="btnCloseDeleteIcon"
                            class="text-slate-400 hover:text-slate-600 transition-colors focus:outline-none">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
                            </svg>
                        </button>
                    </div>

                    <div class="px-6 pb-6 pt-2">
                        <p id="deleteConfirmMessage" class="text-slate-600 text-[15px] leading-relaxed"></p>
                    </div>

                    <div class="px-6 py-5 bg-white flex justify-center space-x-3 mb-2">
                        <button id="btnCancelDelete"
                            class="px-5 py-2.5 border border-slate-300 rounded-xl text-sm font-medium text-slate-700 hover:bg-slate-50 transition-colors focus:outline-none">ยกเลิก
                            (Cancel)</button>
                        <button id="btnConfirmDeleteAction"
                            class="px-5 py-2.5 bg-[#7dcbaa] rounded-xl text-sm font-medium text-white hover:bg-[#68bc94] transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#7dcbaa]">
                            ยืนยันการลบ
                        </button>
                    </div>
                </div>
            </div>

            <!-- Custom Modal Overlay: Lot Details -->
            <div id="lotDetailsModalOverlay"
                class="fixed inset-0 z-50 bg-slate-900/50 backdrop-blur-sm hidden flex items-center justify-center p-4">
                <div id="lotDetailsModalContent"
                    class="bg-white rounded-3xl shadow-xl w-full max-w-lg overflow-hidden animate-fade-in flex flex-col max-h-[90vh]"
                    style="display: none;">

                    <!-- Header -->
                    <div
                        class="px-6 py-5 flex justify-between items-center border-b border-slate-100 bg-white shrink-0">
                        <div>
                            <h2 id="lotDetailsModalTitle" class="text-lg font-bold text-slate-800">รายละเอียดล็อต</h2>
                            <p id="lotDetailsModalSubtitle" class="text-sm text-slate-500 mt-0.5">...</p>
                        </div>
                        <button id="btnCloseLotDetails"
                            class="text-slate-400 hover:text-slate-600 hover:bg-slate-100 p-2 rounded-full transition-colors focus:outline-none focus:ring-2 focus:ring-slate-200">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
                            </svg>
                        </button>
                    </div>

                    <!-- Scrollable Body -->
                    <div class="p-6 overflow-y-auto grow bg-slate-50/50">
                        <!-- Batches container injected via JS -->
                        <div id="lotDetailsModalBody" class="space-y-3">
                        </div>
                    </div>

                </div>
            </div>

            <!-- Pass Auth info to JS safely -->
            <script>
                const AUTH_USER = {
                    username: "<%= locals.user ? locals.user.username : '' %>",
                    role: "<%= locals.user ? locals.user.role : '' %>"
                };
            </script>
            <script src="/js/main.js"></script>
    </body>

</html>