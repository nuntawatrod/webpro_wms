<!DOCTYPE html>
<html lang="th">
<%- include('layout/head') %>

    <body class="bg-slate-50 text-slate-800 antialiased min-h-screen flex flex-col">

        <!-- Navbar -->
        <%- include('layout/navbar', { activePage: 'withdraw' , user: locals.user }) %>

            <!-- Main Content -->
            <main class="flex-grow flex items-center justify-center p-4">

                <div class="bg-white rounded-2xl shadow-xl w-full max-w-lg overflow-hidden border border-slate-100">

                    <div class="bg-slate-50 px-8 py-6 border-b border-slate-100 text-center">
                        <div
                            class="inline-flex items-center justify-center w-12 h-12 rounded-full bg-rose-100 text-rose-600 mb-4">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z">
                                </path>
                            </svg>
                        </div>
                        <h1 class="text-2xl font-bold text-slate-800">เบิกสินค้า</h1>
                        <p class="text-sm text-slate-500 mt-1">เบิกจ่ายสินค้าออกจากคลัง</p>
                    </div>

                    <div class="p-8 space-y-6">

                        <!-- Searchable Dropdown for Available Product -->
                        <div class="relative" id="wdProductContainer">
                            <label class="block text-sm font-medium text-slate-700 mb-1">เลือกสินค้าที่พร้อมเบิก <span
                                    class="text-rose-500">*</span></label>
                            <div class="relative">
                                <input type="text" id="wdProductInput" placeholder="ค้นหาสินค้าที่มีในสต็อก..."
                                    class="w-full pl-3 pr-10 py-3 border border-slate-300 rounded-lg shadow-sm focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 outline-none text-sm transition-all bg-white"
                                    autocomplete="off">
                                <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                    <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor"
                                        viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M19 9l-7 7-7-7"></path>
                                    </svg>
                                </div>
                            </div>
                            <input type="hidden" id="wdSelectedProductId">
                            <div id="wdProductList"
                                class="hidden absolute top-full left-0 right-0 mt-1 bg-white border border-slate-200 rounded-lg shadow-xl z-50 custom-dropdown-menu">
                                <div class="p-3 text-center text-sm text-slate-500">กำลังโหลด...</div>
                            </div>
                        </div>

                        <div class="grid grid-cols-2 gap-5">
                            <!-- Withdraw Date (Auto) -->
                            <div>
                                <label class="block text-sm font-medium text-slate-700 mb-1">วันที่เบิก</label>
                                <input type="text" id="wdDate" readonly
                                    class="w-full px-3 py-3 bg-slate-50 border border-slate-200 text-slate-500 rounded-lg shadow-sm text-sm cursor-not-allowed">
                            </div>

                            <!-- Quantity -->
                            <div>
                                <label class="block text-sm font-medium text-slate-700 mb-1">จำนวนเบิก (ชิ้น/แพ็ค) <span
                                        class="text-rose-500">*</span></label>
                                <input type="number" id="wdQuantity" min="1" placeholder="0"
                                    class="w-full px-3 py-3 border border-slate-300 rounded-lg shadow-sm focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 outline-none text-sm transition-all font-semibold font-mono">
                            </div>
                        </div>

                        <!-- Withdrawer Name -->
                        <div>
                            <label class="block text-sm font-medium text-slate-700 mb-1">ชื่อผู้เบิก <span
                                    class="text-rose-500">*</span></label>
                            <input type="text" id="wdActorName" value="<%= user ? user.username : '' %>" readonly
                                class="w-full px-3 py-3 bg-slate-50 border border-slate-200 text-slate-600 rounded-lg shadow-sm text-sm cursor-not-allowed">
                        </div>
                    </div>

                    <div class="px-8 py-5 bg-slate-50 border-t border-slate-100 flex justify-end space-x-3">
                        <button id="btnWithdrawReset"
                            class="px-5 py-2.5 border border-slate-300 rounded-lg text-sm font-medium text-slate-700 hover:bg-slate-100 transition-colors focus:outline-none focus:ring-2 focus:ring-slate-200">ล้างข้อมูล</button>
                        <button id="btnWithdrawSubmit"
                            class="px-5 py-2.5 bg-rose-600 rounded-lg text-sm font-medium text-white hover:bg-rose-700 shadow-sm transition-all focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-rose-500 disabled:opacity-50 disabled:cursor-not-allowed">
                            ยืนยัน
                        </button>
                    </div>

                </div>
            </main>

            <!-- Notification Area -->
            <div id="toastContainer" class="fixed bottom-4 right-4 z-50 flex flex-col gap-2"></div>

            <script>
                const AUTH_USER = { username: "<%= locals.user ? locals.user.username : '' %>", role: "<%= locals.user ? locals.user.role : '' %>" };
            </script>
            <script src="/js/main.js"></script>
    </body>

</html>