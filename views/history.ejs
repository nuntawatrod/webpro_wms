<!DOCTYPE html>
<html lang="th">
<%- include('layout/head') %>

    <body class="bg-slate-50 text-slate-800 antialiased min-h-screen flex flex-col">

        <!-- Navbar -->
        <%- include('layout/navbar', { activePage: 'history' , user: locals.user }) %>

            <!-- Main Content -->
            <main class="flex-grow max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full">

                <div class="flex flex-col sm:flex-row sm:items-center justify-between mb-6 gap-4">
                    <div>
                        <h1 class="text-2xl font-bold text-slate-900">ประวัติการทำรายการ</h1>
                        <p class="text-sm text-slate-500 mt-1">บันทึกการนำเข้าและเบิกจ่ายสินค้าทั้งหมด</p>
                    </div>
                    <button id="btnExportCsv"
                        class="bg-emerald-600 hover:bg-emerald-700 text-white px-4 py-2 flex-shrink-0 rounded-lg text-sm font-medium shadow-sm shadow-emerald-600/20 transition-all flex items-center focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-emerald-500">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
                        </svg>
                        Export CSV
                    </button>
                </div>

                <!-- Filters -->
                <div
                    class="bg-white p-4 rounded-xl shadow-sm border border-slate-200 mb-6 flex flex-col xl:flex-row xl:items-center gap-4">
                    <!-- Search -->
                    <div class="w-full xl:w-80 flex-shrink-0">
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <svg class="h-4 w-4 text-slate-400" fill="none" viewBox="0 0 24 24"
                                    stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                                </svg>
                            </div>
                            <input type="text" id="histSearchInput"
                                class="block w-full pl-9 pr-3 py-2 border border-slate-300 rounded-lg leading-5 bg-white placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 sm:text-sm transition-colors"
                                placeholder="ค้นหาด้วยชื่อสินค้า หรือผู้ทำรายการ...">
                        </div>
                    </div>

                    <div class="h-px xl:h-8 w-full xl:w-px bg-slate-200"></div>

                    <!-- Date Range -->
                    <div class="flex items-center overflow-x-auto scrollbar-none gap-2 flex-grow">
                        <span class="text-sm font-medium text-slate-500 whitespace-nowrap">ช่วงเวลา:</span>
                        <div class="flex items-center gap-1" id="histDateFilters">
                            <!-- Buttons populated by JS -->
                        </div>
                    </div>
                </div>

                <!-- Type Filters -->
                <div class="mb-4 flex gap-2 overflow-x-auto pb-1 scrollbar-none" id="histTypeFilters">
                    <!-- Tabs populated by JS -->
                </div>

                <!-- History Table -->
                <div class="bg-white shadow-sm border border-slate-200 rounded-xl overflow-hidden">
                    <div class="overflow-x-auto">
                        <table class="w-full text-left border-collapse">
                            <thead>
                                <tr
                                    class="bg-slate-50 border-b border-slate-200 text-xs uppercase tracking-wider text-slate-500 font-semibold">
                                    <th class="px-6 py-4">วัน-เวลา</th>
                                    <th class="px-6 py-4">ประเภทรายการ</th>
                                    <th class="px-6 py-4">ชื่อสินค้า</th>
                                    <th class="px-6 py-4 text-right">จำนวน</th>
                                    <th class="px-6 py-4">ผู้ทำรายการ</th>
                                </tr>
                            </thead>
                            <tbody id="historyTableBody" class="divide-y divide-slate-100">
                                <!-- Rows injected via JS -->
                                <tr>
                                    <td colspan="5" class="px-6 py-8 text-center text-sm text-slate-500">
                                        กำลังโหลดประวัติทำรายการ...
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- History Pagination -->
                <div id="historyPagination" class="mt-4 flex items-center justify-between text-sm text-slate-500"></div>

            </main>

            <script>
                const AUTH_USER = { username: "<%= locals.user ? locals.user.username : '' %>", role: "<%= locals.user ? locals.user.role : '' %>" };
            </script>
            <script src="/js/main.js"></script>
    </body>

</html>